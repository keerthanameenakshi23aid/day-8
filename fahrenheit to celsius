#include <stdio.h>

float fahrenheit_to_celsius(float f) {
    return (f - 32.0) * 5.0 / 9.0;
}

int main() {
    float fahrenheit = 40.0;
    printf("Temperature in Celsius: %.2f\n", fahrenheit_to_celsius(fahrenheit));
    return 0;
}
